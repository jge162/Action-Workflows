name: Hello Action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Say Hello
      run: echo "Hello"
      
    - name: Python Action
      uses: jge162/Action-workflows@2.1.0
