# run file to verify it compiles with no errors 
name: Run Python file
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-analyze:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - run: |
        echo "Install required dependencies"
        sudo apt-get update
        sudo apt-get install python3

    - run: |
        echo "Run, Build Application using scripts"
        python3 -c "
        scripts = ['./script.py', './script2.py']
        for script in scripts:
            with open(script, 'r') as file:
                exec(file.read())
        "

# - run: |
# echo "Set executable permission on script file"
# chmod +x ./build/sh.py
# - run: |
# echo "Run, Build Application using script"
# python3 ./script.py

# - run: |
# echo "Run, Build Application using script2.py"
# python3 ./build/sh.py
# test python3 ./build/sh.py
